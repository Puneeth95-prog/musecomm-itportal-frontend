import React from 'react'

function ComplaintRegMessage() {
    return (
        <section>
            <div style={{minHeight: 'calc(100vh - (94.8px + 58.8px))', padding: '20px 0px 0px 120px'}} className='container complaint-reg-message'>
                <h5>Your Complaint has been registered Successfully</h5>

                
            </div>
            {/* {(localStorage.getItem('loginValue')) && <Hamburger />} */}
        </section>
    )
}

export default ComplaintRegMessage;
